apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: test
subjects:
  - kind: Group
    name: manager
    apiGroups: [""]
roleRef:
    - kind: ClusterRole
      name: secrets-reader
      apiGroup: rbac.authorization.k8s.io/v1

# if we want to have ClusterRoleBinding in Subjects kind should be Group