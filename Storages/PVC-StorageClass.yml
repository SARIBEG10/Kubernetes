apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-st
  namespace: anisa
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: anisa-storage
  resources:
    requests:
      storage: 1Gi